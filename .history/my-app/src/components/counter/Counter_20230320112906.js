import React, { useState } from "react";

const Counter = () => {
  const [count, setCount] = useState(0);
  const handleIncrement = 
  return <div onClick={handleIncrement}>Increasement </div>;
};

export default Counter;
